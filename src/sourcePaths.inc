ARGS_HANDLER=argsHandler

BUFFER=buffer

NET_UTILS=netutils

PARSERS=parsers/*

REFERENCE=reference/*

SELECTOR=selector

SOCKS5=socks5
STATE_MACHINE_BUILDER=$(SOCKS5)/stateMachineBuilder

STATE_MACHINE=stateMachine

STATISTICS=statistics

STATES_PATH=states/*
ERROR_STATES_PATH=states/errorStates/*
CLOSE=states/close/*
STATE_UTILITIES=states/stateUtilities/*

UTILITIES=utilities utilities/*

USER_HANDLER=userHandler

SOURCE_PATHS = $(ARGS_HANDLER) $(BUFFER) $(NET_UTILS) $(PARSERS) $(REFERENCE) $(SELECTOR) $(SOCKS5) $(STATE_MACHINE_BUILDER)
SOURCE_PATHS += $(STATISTICS) $(STATE_MACHINE) $(STATES_PATH) $(ERROR_STATES_PATH) $(CLOSE) $(STATE_UTILITIES) $(UTILITIES) $(USER_HANDLER)
