STATES_PATH=states/*
ERROR_STATES_PATH=states/errorStates/*
BUFFER=buffer
NET_UTILS=netutils
SELECTOR=selector
SOCKS5=socks5
STATE_MACHINE_BUILDER=$(SOCKS5)/stateMachineBuilder
STATE_MACHINE=stateMachine

SOURCE_PATHS = $(STATES_PATH) $(ERROR_STATES_PATH) $(BUFFER) $(NET_UTILS) $(SELECTOR) $(SOCKS5) $(STATE_MACHINE_BUILDER) $(STATE_MACHINE) 
